# Ponytail Fusion Record

Upstream: `DietrichGebert/ponytail`; pinned source commit reviewed: `2ed6c52c9d7e5e56942508591085fd45dea277d3` (2026-08-07). License: MIT, Copyright (c) 2026 DietrichGebert; see `.agents/skills/LICENSE-PONYTAIL`.

This record preserves the semantics of all six upstream skills while keeping Woyengi and the universal contract authoritative.

## `ponytail`
Retained: coding-only lazy-senior minimality; default `full` plus `lite`/`ultra`; stop/off/normal compatibility; understand the real flow before simplifying; ladder `need -> existing code -> stdlib -> native platform -> installed dependency -> direct one-line -> minimum new code`; common-root caller tracing for bugs; no speculative abstractions/factories/config/scaffolding/dependencies; deletion/reuse and boring/short diffs after comprehension; explicit requirements honored; never cut validation/data-loss/security/accessibility/required gates; preserve necessary hardware/physical calibration; non-trivial logic keeps a runnable check; deliberate real ceilings use `ponytail: <ceiling>, <upgrade trigger/path>`; explicitly invoked compatibility mode prefers code/implementation first and concise unrequested prose while fully answering requested reports/walkthroughs.

## `ponytail-review`
Complexity-only diff pass, independent of correctness/security/performance. Tags retained: `delete`, `stdlib`, `native`, `yagni`, `shrink`. Findings identify location, cut, replacement. Minimal smoke/regression checks are not bloat. `net: -N lines possible` is an estimated future cut, not measured historical savings. Nothing material: `Lean already. Ship.` Report only unless fixes are separately authorized.

## `ponytail-audit`
Repo-wide complexity pass. Hunt stdlib/native-duplicating dependencies, one-implementation interfaces, one-product factories, delegate-only wrappers, semantically empty layers/files, dead flags/config, and hand-rolled stdlib. Rank biggest credible cuts first. Keep correctness/security/performance in normal review. Report only unless fixes are authorized.

## `ponytail-debt`
Read-only ledger of language-appropriate `ponytail:` comment markers excluding VCS/dependency/build output. Each row contains file/line, simplification, ceiling, upgrade trigger/path. Missing trigger -> `no-trigger`. Optional owner from blame/history. Persist only when requested. End with marker/no-trigger counts.

## `ponytail-gain`
Honesty boundary: benchmark figures are upstream measurements, never live-repo savings without a controlled baseline. Pinned upstream preserves two methodologies: (1) later agentic README benchmark: 12 feature tasks, n=4, Haiku 4.5, approx LOC -54%, tokens -22%, cost -20%, time -27%, safety 100% on that benchmark; (2) legacy isolated-generation gain card: 5 tasks across Haiku/Sonnet/Opus, reported lines -80–94%, cost -47–77%, speed 3–6x. Keep methods separate and never blend/extrapolate. One-shot; changes no mode/state.

## `ponytail-help`
Compatibility triggers retained: ponytail lite/full/ultra, ponytail-review, ponytail-audit, ponytail-debt, ponytail-gain, ponytail-help, plus stop/off/normal-mode phrases. Upstream plugin config preserved as provenance only: `PONYTAIL_DEFAULT_MODE`; `~/.config/ponytail/config.json` (Windows `%APPDATA%\ponytail\config.json`); env > config > full. Host-specific slash/@ and plugin-update mechanics remain upstream concerns; the Woyengi repo-local fusion is Chat/Work/tool independent.

## Fusion into existing skills
`ask-matt` routes simplification work; `implement` applies the ladder; `diagnosing-bugs` traces common-root callers; `tdd` reconciles falsifier-first with minimum checks; `code-review` adds an independent complexity axis; `improve-codebase-architecture` gains repo-wide audit heuristics; `handoff` tracks deliberate simplification debt. All other adapted skills inherit the minimality discipline through the universal contract without duplicating the rule source.
