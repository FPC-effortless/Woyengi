---
name: ponytail
description: Apply the universal lazy-senior minimality discipline to a coding task; supports lite, full, and ultra intensity.
argument-hint: "[lite|full|ultra]"
license: MIT
---
# ponytail
Compatibility alias for `.agents/universal/CONTRACT.md`. Default `full`; `lite` builds the requested shape while naming a materially simpler option; `full` enforces the ladder; `ultra` aggressively rejects speculative additions/deletes before adding while honoring explicit behavior and all Woyengi safety/state/verification rules.

Read/reconstruct first. Then: need at all → existing Woyengi primitive → stdlib → native platform → installed dependency → direct one-line form → minimum new code. Fix bugs at common root seams. No speculative abstraction/config/scaffolding. Mark real deliberate ceilings with `ponytail: <ceiling>, <upgrade trigger/path>`.

Never cut validation, data-loss protection, security/privacy, accessibility, constitutional invariants, evidence/authority/reconciliation, required release gates, or necessary hardware/physical calibration. Non-trivial logic requires a runnable check. `stop ponytail`, `ponytail off`, or `normal mode` disables only this minimality preference.
