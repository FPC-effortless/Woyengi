---
name: ask-matt
description: Route engineering, RL-environment, and research work through the universal fused workflow.
---
# ask-matt

Read `/AGENTS.md`, `/CONTEXT.md`, `.agents/universal/CONTRACT.md`, `.agents/woyengi/OVERLAY.md`, `docs/agents/modes.md`, and the task's durable artifacts first.

Route by uncertainty:
- unknown facts -> `research`;
- unresolved decisions -> `grill-with-docs`;
- huge/foggy work -> `wayfinder`;
- settled direction -> `to-spec`;
- multi-session work -> `to-tickets`;
- implementation -> `implement`;
- bug -> `diagnosing-bugs`;
- incoming issue/PR -> `triage`;
- architecture health -> `improve-codebase-architecture`;
- general review -> `code-review`;
- over-engineering diff -> `ponytail-review`;
- repo-wide bloat -> `ponytail-audit`;
- deliberate simplification ledger -> `ponytail-debt`;
- context transfer -> `handoff`;
- human-only secrets/setup -> `wizard`.

The Ponytail minimality ladder applies to coding routes by default. Do not route around Woyengi's authority, history, or verification constraints. If a skill conflicts with the Constitution, the skill loses.

For product engineering, prefer one end-to-end governed vertical slice over broad speculative breadth. For RL work, define hidden ground truth, environment dynamics, verifier, and leakage controls before optimization. For research, require a falsifiable hypothesis and baseline before interpreting results.
