# Woyengi Platform Progress

## 2026-08-13 — IDEA / RESEARCH / PRD

- Received and adopted the persistent reconstructable state architecture.
- Confirmed the workspace was empty and had no Git history or baseline tests.
- Chose a dependency-light TypeScript + Node 24 + pnpm foundation.
- Recorded official runtime and monorepo constraints in `research.md`.
- Created the initial executable ticket sequence in `prd.json`.
- Next: PLAT-001 repository bootstrap, followed by the canonical state vertical slice.

## 2026-08-13 — PLAT-001 GREEN

- Initialized the repository on `main`.
- Added the Node 24 / pnpm workspace contract and root test commands.
- Added the constitution, PRD, research log, README, and agent workflow.
- Verification: `pnpm test` passed with the empty-suite bootstrap contract.
- Next: PLAT-002 canonical record contracts.

## 2026-08-13 — PLAT-002 RED → GREEN

- RED: `packages/core/test/records.test.ts` failed because the canonical record API did not exist.
- GREEN: added immutable domain-neutral Observation, Claim, Evidence, Provenance, Authority, Lifecycle, and bitemporal contracts.
- Enforced namespaced opaque IDs, explicit timestamp offsets, valid intervals, and separate confidence/authority values.
- Verification: `pnpm test` passed on Node v24.19.0 (1 test).
- Next: PLAT-003 append-only claim ledger.

## 2026-08-13 — PLAT-003 RED → GREEN

- RED: the ledger behavior test failed because `ClaimLedger` did not exist.
- GREEN: added append-only in-memory claim storage with duplicate-ID rejection.
- History queries filter by subject/predicate and sort deterministically by transaction time then record ID.
- Verification: `pnpm test` passed on Node v24.19.0 (2 tests).
- Next: PLAT-004 bitemporal state projection, conflicts, and trace.

## 2026-08-13 — PLAT-004 RED → GREEN → RED → GREEN

- RED 1: projection test failed because `projectAt` did not exist.
- GREEN 1: added explicit valid-time/transaction-time filtering, lifecycle/authority ranking, conflict retention, and a five-stage trace.
- RED 2: lifecycle test failed because append-only transitions did not exist.
- GREEN 2: added provenance-ready lifecycle transition records plus transaction-time-aware retraction and supersession.
- Original claims remain in ledger history after lifecycle changes.
- Verification: `pnpm test:coverage` passed on Node v24.19.0 (4 tests; 88.59% line, 70.33% branch, 90.70% function coverage).
- Next: PLAT-005 deterministic replay and projection rebuilding.

## 2026-08-13 — FOUNDATION QA HANDOFF

- Preserved the complete target architecture and Platform/Compute boundary in `docs/architecture.md`.
- Added `QA.md` with human scenarios, automated commands, limitations, and the regression restart protocol.
- Foundation automated checks are green; human QA acknowledgement remains pending.

## 2026-08-13 — GOAL CONTINUATION / FULL PRD

- User directed continued execution until the complete platform goal is achieved.
- Activated persistent goal tracking for the full architecture and production-ready handoff.
- Expanded `prd.json` from 8 to 40 dependency-ordered tickets covering every target layer.
- Moved the active milestone to canonical integrity and replay (PLAT-005, PLAT-006, PLAT-009 through PLAT-012).
- Next: PLAT-005 deterministic replay.

## 2026-08-13 — PLAT-005 RED → GREEN

- RED: replay test failed because no canonical replay API existed.
- GREEN: added deterministic ordering, transaction-time cutoff replay, canonical record export, and clean-ledger rebuilding.
- One implementation defect (`Iterator.flat`) was corrected after the first green attempt; the second run passed.
- Verification: `pnpm test` passed on Node v24.19.0 (5 tests).
- Next: PLAT-009 compiler and package-boundary gates, then PLAT-006 identity operations.

## 2026-08-13 — PLAT-009 BLOCKED ATTEMPT 1

- RED: `pnpm typecheck` failed because no compiler script or dependency exists.
- The online registry request remained unresponsive and was terminated without partial changes.
- Offline pnpm installation also failed because the TypeScript tarball was not present in the local pnpm store.
- PLAT-009 remains pending; independent work continued on PLAT-006.

## 2026-08-13 — PLAT-006 RED → GREEN

- RED: identity replay test failed because the identity registry API did not exist.
- GREEN: added provenance-backed entity creation, aliases/external IDs, provisional match proposals, authoritative merges, reversible splits, deterministic replay, and merge history.
- Split replay restores original alias ownership without erasing merge history.
- Verification: `pnpm test` passed on Node v24.19.0 (6 tests).
- Next: retry PLAT-009 when registry access is available; PLAT-007 is now the next independent unblocked ticket.

## 2026-08-13 — PLAT-007 RED → GREEN

- RED: domain package test failed because the Domain SDK contract did not exist.
- GREEN: added immutable versioned Domain Packages covering types, graphs, lifecycle, authority, reducers, verification, reconstruction, permissions, procedures, and connectors.
- Registry enforces platform API compatibility, namespace-qualified definition IDs, duplicate protection, and deterministic installed-package order.
- Verification: `pnpm test` passed on Node v24.19.0 (7 tests).
- Next: PLAT-009 remains the only unblocked prerequisite for canonical record expansion; retry compiler installation.

## 2026-08-13 — PLAT-015 RED → GREEN

- RED: capability test failed because the permission engine did not exist.
- GREEN: added default-deny capability evaluation across principal, boundary-safe resource prefixes, graph, entity, operation, purpose, sensitivity, conditions, and time.
- Delegation is constrained to the parent's issuer, time, operations, purposes, resources, graph/entity scope, sensitivity, and conditions.
- `RECONSTRUCT` permission is proven not to imply `PROPOSE_WRITE`.
- Verification: `pnpm test` passed on Node v24.19.0 (8 tests).
- Next: retry PLAT-009; no other canonical-integrity ticket is unblocked until the compiler gate passes.

## 2026-08-13 — PLAT-009 BLOCKED ATTEMPTS 2–3 / PRD EDGE CORRECTION

- A bounded registry retry downloaded `@types/node` and `undici-types` but repeatedly timed out on the TypeScript artifact.
- A locally installed TypeScript package was repacked only to seed the pnpm store; pnpm correctly rejected it as a substitute for the official registry-integrity artifact.
- A long-timeout official fetch also failed at the transport layer.
- No machine-specific path or unverifiable dependency was added to the repository.
- Corrected PLAT-010 so semantic canonical-record work no longer depends on this cross-cutting delivery gate; PLAT-009 remains required and open.
- Next: PLAT-010 canonical events, relationships, decisions, and artifacts.

## 2026-08-13 — PLAT-010 RED → GREEN

- RED: canonical record test failed because event, relationship, decision, and artifact constructors did not exist.
- GREEN: added immutable bitemporal records for typed events/participants, authoritative relationships, decisions, and content-addressed artifacts.
- Artifacts retain original storage plus exact residual-detail locators; all derived records retain evidence and provenance.
- Verification: `pnpm test` passed on Node v24.19.0 (9 tests).
- Next: PLAT-011 generalized canonical ledger.

## 2026-08-13 — PLAT-011 RED → GREEN

- RED: generalized ledger test failed because no canonical ledger port existed.
- GREEN: added a storage-agnostic append/query/get port and immutable in-memory adapter accepting every structural record kind.
- IDs are unique across kinds; replay and queries sort by transaction time then ID and support record-kind/time filters.
- Verification: `pnpm test` passed on Node v24.19.0 (10 tests).
- Next: PLAT-012 provenance DAG and invalidation propagation.

## 2026-08-13 — PLAT-012 RED → GREEN

- RED: provenance test failed because the DAG API did not exist.
- GREEN: added deterministic DAG construction, upstream/downstream traversal, missing-reference and cycle rejection, and immutable invalidation impact records.
- Invalidated sources mark every dependent observation, claim, projection, and reconstruction unsupported without deleting history.
- Verification: `pnpm test` passed on Node v24.19.0 (11 tests).
- Next: PLAT-013 evidence relationships and verification state.

## 2026-08-13 — PLAT-013 RED → GREEN

- RED: evidence-engine test failed because support/contradiction links and verifier outcomes did not exist.
- GREEN: added immutable evidence links, exact-locator retention, append-only verification outcomes, and explainable supported/contradicted/mixed/unresolved summaries.
- Summarization retains every candidate and verification without mutating source evidence.
- Verification: `pnpm test` passed on Node v24.19.0 (12 tests).
- Next: PLAT-014 contextual authority evaluation.

## 2026-08-13 — PLAT-014 RED → GREEN

- RED: authority-ranking test failed because no contextual authority engine existed.
- GREEN: added policy evaluation across principal, predicate, purpose, conditions, and time, with deterministic ranking and per-policy traces.
- A lower-confidence executive decision correctly outranks a high-confidence model inference because authority and confidence remain independent.
- Verification: `pnpm test` passed on Node v24.19.0 (13 tests).
- Next: PLAT-016 extensible verification engine.

## 2026-08-13 — PLAT-016 RED → GREEN

- RED: verification test failed because the shared verifier protocol did not exist.
- GREEN: added schema, constraint, source, cross-source, temporal, authority, consistency, external, human, and domain strategy support.
- Rejections, missing verifiers, and thrown verifier errors become explicit outcomes; aggregate decisions are immutable canonical ledger records.
- Verification: `pnpm test` passed on Node v24.19.0 (14 tests).
- Next: PLAT-017 typed graph federation.

## 2026-08-13 — PLAT-017 RED → GREEN

- RED: graph-registry test failed because typed graph definitions and materialization did not exist.
- GREEN: added versioned node/edge schemas, invariants, temporal behavior, retention, permission operations, verifier hooks, compatibility checks, and deterministic ledger rebuilding.
- Backward-incompatible minor updates are rejected and require a major graph version.
- Verification: `pnpm test` passed on Node v24.19.0 (15 tests).
- Next: PLAT-018 permission-aware cross-graph bindings.

## 2026-08-13 — PLAT-018 RED → GREEN

- RED: binding traversal test failed because the binding graph did not exist.
- GREEN: added immutable cross-graph bindings and breadth-first traversal with direction, depth, valid-time, transaction-time, permission, and cycle controls.
- Traversal records every traversed, denied, out-of-time, or already-visited edge in an inspectable trace.
- Verification: `pnpm test` passed on Node v24.19.0 (16 tests).
- Next: PLAT-019 guarded procedure execution.

## 2026-08-13 — PLAT-019 RED → GREEN → REFACTOR

- RED: procedure execution test failed because structural procedure definitions and guarded execution did not exist.
- GREEN: added preconditions, operations/tools, invariants, verification, postconditions, repair behavior, permission-first execution, and immutable traces.
- Verified successful runs can emit a versioned candidate procedure tied to the originating execution.
- REFACTOR: extracted the execution ports contract while retaining green tests.
- Verification: `pnpm test` passed on Node v24.19.0 (17 tests).
- Next: PLAT-020 storage ports and durable local adapters.

## 2026-08-13 — PLAT-020 RED → GREEN

- RED: local storage restart test failed because storage ports and adapters did not exist.
- GREEN: added ledger, graph, object, search, vector, and cache ports plus durable local canonical-ledger and content-addressed object-store adapters.
- Ledger appends serialize through an atomic write queue, reject duplicate IDs, survive restart, and restore deterministic ordering; objects verify SHA-256 before commit.
- Verification: `pnpm test` passed on Node v24.19.0 (18 tests).
- Next: PLAT-021 durable event bus and subscriber cursors.

## 2026-08-13 — PLAT-021 RED → GREEN

- RED: crash/restart delivery test failed because the platform event bus did not exist.
- GREEN: added typed state-change events, durable local event storage, topic subscriptions, atomic subscriber cursors, deterministic delivery IDs, and at-least-once consumption.
- A handler crash leaves the cursor unchanged; restart redelivers the same identifiable event and loses nothing.
- Verification: `pnpm test` passed on Node v24.19.0 (19 tests).
- Next: PLAT-022 ingestion fabric and connector envelope.

## 2026-08-13 — PLAT-022 RED → GREEN

- RED: ingestion-envelope test failed because the common ingestion fabric did not exist.
- GREEN: added one envelope for documents, application events, API payloads, agent proposals, and human actions with source identity, SHA-256 content address, storage policy, lifecycle, and transaction time.
- Concurrent and repeated idempotency keys return the original receipt without duplicating ledger, object, or event writes.
- Verification: `pnpm test` passed on Node v24.19.0 (20 tests).
- Next: PLAT-023 multimodal retrieval orchestration.

## 2026-08-13 — PLAT-023 RED → GREEN

- RED: retrieval-orchestration test failed because the shared modality protocol did not exist.
- GREEN: added lexical, vector, graph, temporal, entity, state, procedure, and evidence providers with weighted deterministic fusion.
- Every result retains provider/modality contribution scores and index provenance; retrieval remains a candidate mechanism, not canonical state.
- Verification: `pnpm test` passed on Node v24.19.0 (21 tests).
- Next: PLAT-024 semantic decomposition and entity-resolution proposals.

## 2026-08-13 — PLAT-024 RED → GREEN → REFACTOR

- RED: semantic-compilation test failed because the compiler boundary did not exist.
- GREEN: added a pluggable decomposition protocol producing provisional claims, events, relationships, evidence locators, and scored identity candidates.
- Every proposal retains an exact validated source span; the compiler has no governing-state mutation behavior.
- REFACTOR: extracted a clean compiler ports interface while retaining green tests.
- Verification: `pnpm test` passed on Node v24.19.0 (22 tests).
- Next: PLAT-025 deterministic state-requirement planning.

## 2026-08-13 — PLAT-025 RED → GREEN

- RED: planning test failed because no state-requirement planner existed.
- GREEN: added normalized intent planning for subjects, actions, domains, valid/knowledge time, constraints, evidence, contradictions, graphs, retrieval modalities, authority, and permission checks.
- Equivalent normalized requests produce byte-equivalent plans with deterministic ordering.
- Verification: `pnpm test` passed on Node v24.19.0 (23 tests).
- Next: PLAT-008 structured reconstructive workspace.

## 2026-08-13 — PLAT-008 RED → GREEN

- RED: reconstruction test failed because the workspace engine did not exist.
- GREEN: added permission-first reconstruction that composes planned graphs/retrieval into structured current state, history, events, decisions, constraints, procedures, evidence, conflicts, uncertainty, authority, provenance, and rendered context.
- Every workspace is an immutable canonical reconstruction record with an eight-stage trace from intent through context assembly.
- Verification: `pnpm test` passed on Node v24.19.0 (24 tests).
- Next: PLAT-026 guarded agent proposals and actions.

## 2026-08-13 — PLAT-026 RED → GREEN

- RED: agent protocol test failed because the guarded gateway did not exist.
- GREEN: added separately authorized reads, proposed writes, validation, authority assessment, optional verification, lifecycle assignment, and procedure-bound actions.
- Agent actions retain request, capability decision, procedure, result, provenance, principal, and transaction time; denied writes append nothing.
- Verification: `pnpm test` passed on Node v24.19.0 (25 tests).
- Next: PLAT-027 stable Domain, Agent, and Connector SDK surfaces.

## 2026-08-13 — PLAT-027 RED → GREEN → REFACTOR

- RED: SDK contract test failed because Connector SDK and explicit SDK versions did not exist.
- GREEN: exposed versioned Domain, Agent, and Connector contracts plus Platform API range enforcement, connector cursoring, and stable external-record idempotency keys.
- REFACTOR: extracted the connector delivery contract and documented semantic-versioning, compatibility, deprecation, and service-internal boundaries.
- External registry publishing remains correctly gated by PLAT-009 compiled artifacts.
- Verification: `pnpm test` passed on Node v24.19.0 (26 tests).
- Next: PLAT-028 local Platform HTTP API.

## 2026-08-13 — PLAT-028 RED → GREEN

- RED: HTTP integration test failed because the Platform API service did not exist.
- GREEN: added a runnable Node HTTP API for INGEST, STATE, RECONSTRUCT, and CONTROL with authentication, per-route capability checks, 1 MiB input bounds, idempotency, pagination, and trace IDs.
- Success and error responses share stable envelopes; unauthenticated/unauthorized requests reach no domain handler.
- Verification: `pnpm test` passed on Node v24.19.0 (27 tests, including real loopback HTTP).
- Next: PLAT-029 TypeScript and Python client SDKs.

## 2026-08-13 — PLAT-029 RED → GREEN → REFACTOR

- RED: both client tests failed because TypeScript and Python SDKs did not exist.
- GREEN: added ingestion, state, reconstruction, control, and subscription clients driven by a shared HTTP contract fixture.
- Both clients reuse caller-supplied idempotency keys across network/server retries and parse the same response envelope.
- REFACTOR: replaced a hanging nested-pnpm test script with a direct cross-language gate and portable Python-runtime discovery.
- Verification: `pnpm test:all` passed (28 Node tests plus 1 Python unittest).
- Next: PLAT-030 storage policies and local/cloud/hybrid synchronization.

## 2026-08-13 — PLAT-030 RED → GREEN

- RED: synchronization test failed because storage policies and merge semantics did not exist.
- GREEN: added locality, device, region, agent, encryption, retention, and expiry policies plus deterministic operation synchronization.
- Mergeable maps converge independent of arrival order; concurrent authoritative heads stop as explicit conflicts; local-only/ephemeral content is rejected before cloud transfer.
- Verification: `pnpm test` passed on Node v24.19.0 (29 tests).
- Next: PLAT-031 provenance-aware deletion and invalidation propagation.

## 2026-08-13 — PLAT-031 RED → GREEN

- RED: deletion-coordinator test failed because the lifecycle deletion boundary did not exist.
- GREEN: added permission-checked, retention-aware source deletion that invalidates the complete provenance-dependent subgraph, purges unsupported IDs from indexes, and notifies consumers.
- Logical invalidation retains the source for governed audit history; physical erasure removes it only after retention policy permits. Both paths append an immutable source-invalidation record.
- Verification: `pnpm test` passed on Node v24.19.0 (30 tests).
- Next: PLAT-032 replay, inspection, migration, backup, restore, and integrity CLI.

## 2026-08-13 — PLAT-032 RED → GREEN

- RED: operator CLI test failed because no executable command surface existed.
- GREEN: added `replay`, `inspect`, `migrate`, `backup`, `restore`, and `integrity` commands over durable workspace state.
- Replay exports deterministic transaction-time snapshots; inspections cover provenance, conflicts, permissions, and reconstructions; content-hashed backups restore byte-identical workspaces and reject unsafe paths or non-empty targets.
- Verification: `pnpm test` passed on Node v24.19.0 (31 tests).
- Next: PLAT-033 Woyengi Explorer.
