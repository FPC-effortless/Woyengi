# Production requirements evidence

The authoritative scope is `prd.json`; the architectural invariants are `CONSTITUTION.md`. A release run must fail when either artifact is absent or malformed. PLAT-040 additionally requires every PRD ticket to pass and explicit human QA acknowledgement.
