# Claude Code entrypoint

Follow `AGENTS.md` as the repository operating contract. It extends the existing `agent.md` and points to the adapted skills in `.agents/skills/`.

If your client does not auto-discover `.agents/skills`, read the relevant `SKILL.md` directly. Do not duplicate or fork the skill instructions into a second source of truth.
